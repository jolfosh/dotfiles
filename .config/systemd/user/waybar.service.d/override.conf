# Wait out the uwsm/Hyprland startup race instead of failing fast.
# Stock unit retries 5x in ~1s and trips start-limit before WAYLAND_DISPLAY exists.
[Unit]
StartLimitIntervalSec=60
StartLimitBurst=10

[Service]
Restart=on-failure
RestartSec=2
